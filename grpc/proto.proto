syntax = "proto3";
option go_package = "ricart-argawala/grpc";

message Message {
  int64 lamport_timestamp = 1;
  string from = 2;
  string message = 3;
}

message Ack{
  string from = 1;
  string status = 2;
}

service Node {
  rpc MessageStream (stream Message) returns (stream Ack);
}